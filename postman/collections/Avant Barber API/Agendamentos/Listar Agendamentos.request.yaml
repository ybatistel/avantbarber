$kind: http-request
name: Listar Agendamentos
url: "{{baseUrl}}/agendamentos"
method: GET
order: 1785266163510
description: Lista todos os agendamentos. Requer login (barbeiro/admin) — não é consumido pela landing page.
